
<div class="">
    <div class="md:max-w-2/3 z-20 relative mb-6 md:mb-0">
        {% include '_partials/entry/headline.twig' %}
    </div>
    <div class="grid md:grid-cols-2 gap-12 md:gap-24 md:-mt-8 z-10">
        {% if entry.image is defined and entry.image is not null %}
            <div class="{{ entry.position == 'left' ? 'order-0' : 'order-1' }}" data-aos="fade-up">
                {{ include('_partials/entry/image.twig') }}
            </div>
        {% endif %}

        <div class="md:mt-16 xl:mt-32" data-aos="fade-up">

            {% include '_partials/entry/richText.twig' %}
        </div>
    </div>
</div>
